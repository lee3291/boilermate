basically put anything that you want to reuse again and it is somewhat constant or a function

common/
    ├── middleware/
    │   └── logger.middleware.ts  # Logs every request path/time
    ├── filters/
    ├── guards/
    ├── pipes/
    └── utils/
