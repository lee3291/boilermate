model Listing {
  listingID        String      @id @default(uuid())
  userID           String
  title            String
  description      String
  pricing          Float
  media            String[]
  status           Status
  viewCount        Int         @default(0)
  createdAt        DateTime    @default(now())
}

enum Status {
  ACTIVE
  INACTIVE
  ARCHIVED
}
