model User {
  id               String      @id @default(uuid())
  email            String      @unique
  hashedPassword   String
  isVerified       Boolean     @default(false)
  listings         Listing[]
}