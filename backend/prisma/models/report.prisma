model Report {
  id              Int       @id @default(autoincrement())
  reporterId      Int
  reportedUserId  Int
  reason          String
  comments        String
  status          String    @default("unresolved")
  createdAt       DateTime  @default(now())
}

